import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  bannerListContainer: {
    paddingHorizontal: 12,
    marginTop: 24,
  },
  bannerContainer: {
    marginRight: 8,
  },
});
